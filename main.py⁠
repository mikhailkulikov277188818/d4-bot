import os
import logging
import asyncio
from aiogram import Bot, Dispatcher, types
from aiogram.filters import Command

# Твой токен от BotFather
TOKEN = "8985836671:AAHEkBKndw3qmQckEvE-T2DsNV0N8h03gW8"

# Базовая база знаний по Diablo 4 (для старта)
D4_DATABASE = (
    "📊 База знаний Diablo 4:\n\n"
    "⚔️ Актуальная мета и советы:\n"
    "1. Капы статов: Броня — 9230, Сопротивления — 70% на Томенте.\n"
    "2. Закаливание (Tempering): Всегда проверяй приоритет урона и уязвимости на шмотках.\n"
    "3. S-Tier классы текущего сезона: Фокусируемся на сборках через мощные уникалки и правильные Аспекты."
)

logging.basicConfig(level=logging.INFO)
bot = Bot(token=TOKEN)
dp = Dispatcher()

@dp.message(Command("start"))
async def cmd_start(message: types.Message):
    await message.answer(
        "Привет, геймер! 🎮 Я твой ИИ-помощник по Diablo 4.\n"
        "Я знаю всё про актуальные билды, капы статов и мету.\n\n"
        "Напиши свой вопрос по классу, билду или шмоту, и я подскажу!"
    )

@dp.message()
async def answer_user(message: types.Message):
    user_text = message.text.lower()
    
    # Простой логический ответ на основе нашей базы
    if "билд" in user_text or "класс" in user_text or "мета" in user_text:
        response = f"🎯 Ответ по твоему запросу:\n\n{D4_DATABASE}\n\n*Скоро здесь подключится полноценный ИИ-аналитик патчей!*"
    else:
        response = (
            "Я принял твой вопрос в работу! Сейчас база данных затачивается под этот запрос. "
            "Спроси меня про билд, мету или капы статов для Diablo 4."
        )
    
    await message.answer(response)

async def main():
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
